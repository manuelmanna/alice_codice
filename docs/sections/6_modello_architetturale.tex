\section{Modello Architetturale}

A.L.I.C.E. e' una web application full-stack basata su \textbf{Next.js App Router} e \textbf{Supabase}. Il progetto non usa un application server separato: Next.js gestisce pagine, componenti server, componenti client, Server Actions, route API e proxy; Supabase gestisce autenticazione, database PostgreSQL, API PostgREST e Row Level Security.

\subsection{Architettura hardware}

L'architettura hardware del sistema e' composta da tre tipologie di nodi fisici: il dispositivo dell'utente finale, il server applicativo remoto e il server database remoto. Un quarto nodo esterno fornisce il servizio di intelligenza artificiale. Tutti i nodi remoti risiedono in infrastrutture cloud gestite da terze parti; il sistema non richiede server fisici dedicati.

\begin{table}[H]
\centering
\renewcommand{\arraystretch}{1.25}
\begin{tabularx}{\textwidth}{|l|X|}
\hline
\textbf{Nodo hardware} & \textbf{Ruolo nel sistema} \\
\hline
Dispositivo client & PC, laptop o tablet dell'utente (operatore o paziente). Esegue il browser e invia le richieste HTTP al server applicativo. \\
\hline
Server applicativo (cloud) & Server remoto che ospita ed esegue l'applicazione web. Riceve le richieste HTTP, elabora la logica applicativa e restituisce le risposte al client. \\
\hline
Server database (cloud) & Server remoto che ospita il database relazionale. Memorizza in modo persistente tutti i dati applicativi e gestisce autenticazione e controllo degli accessi a livello di riga. \\
\hline
Server API esterna (cloud) & Server remoto di terze parti che espone un servizio di intelligenza artificiale. Riceve richieste dal server applicativo e restituisce suggerimenti testuali. \\
\hline
\end{tabularx}
\caption{Nodi hardware del sistema}
\label{tab:arch-hw}
\end{table}

\subsection{Modello architetturale}

Il modello architetturale non e' un MVC classico. La separazione delle responsabilita' e' ottenuta tramite il modello di Next.js App Router:

\begin{itemize}
    \item le \textbf{pagine} definiscono le viste raggiungibili dall'utente;
    \item i \textbf{Server Components} caricano dati e preparano la UI lato server;
    \item i \textbf{Client Components} gestiscono interazioni complesse nel browser;
    \item il \textbf{proxy} gestisce sessione e redirect delle aree private;
    \item Supabase rappresenta il livello dati e applica le policy di visibilita'.
\end{itemize}

Questa architettura riduce il numero di file necessari rispetto a un backend separato e mantiene la logica vicino alle pagine che la utilizzano.

\subsection{Architettura software}

Il sistema e' sviluppato con le seguenti tecnologie:

\begin{itemize}
    \item \textbf{Next.js 16} (App Router): framework full-stack che gestisce routing, rendering lato server, Server Actions e proxy di autenticazione.
    \item \textbf{React 19}: libreria per la costruzione dei componenti dell'interfaccia utente.
    \item \textbf{TypeScript 5}: tipizzazione statica del codice applicativo.
    \item \textbf{CSS Modules / CSS globale}: stilizzazione delle pagine senza librerie UI esterne.
    \item \textbf{Supabase} (\texttt{@supabase/ssr} 0.8.0): piattaforma Backend-as-a-Service che fornisce autenticazione, database PostgreSQL, API REST e policy Row Level Security.
    \item \textbf{Groq API}: servizio esterno per la generazione del suggerimento testuale per l'operatore, invocato lato server tramite \texttt{fetch}.
\end{itemize}

\subsection{DBMS, browser e sistemi supportati}

Il DBMS e' PostgreSQL tramite Supabase. Il codice applicativo usa il client Supabase per leggere e scrivere dati, mentre le regole RLS nel database impediscono l'accesso a dati non autorizzati. L'applicazione e' fruibile da browser moderni su Windows, macOS, Linux, Android e iPadOS. Per il paziente e' consigliato un tablet o un PC con schermo ampio; per l'operatore e' sufficiente un PC, laptop o tablet.

\newpage
